export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  DIVERSE = 'gender diverse',
  PREFER_NOT_TO_SAY = 'prefer not to say',
}

export enum UserStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  BANNED = 'banned',
}

export enum UserRole {
  CUSTOMER = 'customer',
  PROVIDER = 'provider',
  ADMIN = 'admin',
}
